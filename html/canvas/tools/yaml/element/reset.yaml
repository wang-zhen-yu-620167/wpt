- name: 2d.reset.basic
  desc: reset clears to transparent black
  code: |
    ctx.fillStyle = '#f00';
    ctx.fillRect(0, 0, 100, 50);
    ctx.reset();
    @assert pixel 50,25 == 0,0,0,0;
    t.done();